<view wx:if="{{!hide}}">
    <button bindtap="displayText">查看/隐藏身份信息</button>
</view>

<view wx:if="{{hide}}">
    <button bindtap="hideRole">查看/隐藏身份信息</button>
</view>

<view wx:if="{{roleNo!=-1 && hide}}">
{{roleNo}}{{text}}
</view>